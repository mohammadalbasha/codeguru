import { Expenses } from "../../components/expenses/expenses";

export function ExpensesPage() {
  return (
    <div className="   bg-gradient-to-r from-cyan-500 to-cyan-800     flex flex-col items-center justify-start">
      <Expenses />
    </div>
  );
}
